
import { checkId } from 'catsle/helpers/check-id';
import { module, test } from 'qunit';

module('Unit | Helper | check id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkId([42]);
  assert.ok(result);
});

